# RegexToDfa

A .NET 8 console project that takes a regular expression, converts it to postfix form, builds an NFA-λ, then converts it to a DFA and allows testing whether words are accepted. It can also export the automaton to a text file for inspection.
